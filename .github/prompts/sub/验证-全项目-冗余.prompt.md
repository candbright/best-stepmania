# 用途
用于**验证整个项目**中的死代码与冗余设计问题，并给出治理优先级。

# 适用范围（仅此文件定义边界）
1. 覆盖 `src/` 下前端代码、i18n 资源、以及相关配置与测试目录。
2. 默认排除 `dist/`、`node_modules/`、`target/` 等非源码目录。
3. 结论必须可追溯到代码证据。

# 实际规则（须完整遵循）
@.github/prompts/sub/验证-规则-冗余.prompt.md

执行时采用该文件中的**「全项目模式」**（角色、审查清单、输出格式、执行原则）。
