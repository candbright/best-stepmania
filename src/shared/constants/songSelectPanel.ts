/** Default width for select-music left panel (px); matches config default and drag minimum baseline. */
export const SONG_SELECT_PANEL_WIDTH_DEFAULT_PX = 320;
/** Minimum width reserved for the right-hand detail panel when dragging (px). */
export const SONG_SELECT_PANEL_MIN_DETAIL_PX = 400;
